import 'package:flutter/material.dart';

class IncompleteTodosScreen extends StatelessWidget {
  IncompleteTodosScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: <Widget>[
          Text('Incomplete todos Screen')
        ],
      ),
    );
  }
}
