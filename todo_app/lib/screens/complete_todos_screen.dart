import 'package:flutter/material.dart';

class CompleteTodosScreen extends StatelessWidget {
  CompleteTodosScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: <Widget>[
          Text('Complete Todos Screen')
        ],
      ),
    );
  }
}
